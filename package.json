{
  "name": "website-transmares",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:site": "vite build",
    "build:hub": "vite build --config vite.hub.config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.0"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
